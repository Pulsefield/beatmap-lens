# Changesets

Add a changeset for every user-visible package change:

```bash
pnpm changeset
```

Choose the affected package and semantic version bump, then describe the change in language
that will be useful in release notes. Documentation-only and repository-maintenance changes do
not need a changeset unless they alter the published package.
