[project]
name = "beatmap-lens-tools"
version = "0.0.0"
description = "Annotation, harness, and evaluation scripts for Beatmap Lens."
requires-python = ">=3.10,<3.11"
dependencies = [
    "mcp==1.29.1",
    "pyarrow==22.0.0",
    "Pillow==11.3.0",
]

[tool.uv]
package = false
